function showData () {

    // loop
    // variable declare      var a;
    // variable initialize   a = 10
    // variable condition    --
    // variable increament   a++
    for (var a = 1; a < 10; a++) {
        console.log(a);
    }
}